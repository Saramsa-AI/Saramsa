APPLICATIONINSIGHTS_CONNECTION_STRING="your-app-insights-connection-string"
SECRET_KEY='your-django-secret-key'

# Allowed hosts (comma-separated). Required when DEBUG=False.
ALLOWED_HOSTS=localhost,127.0.0.1,api.saramsa.ai

# CORS / CSRF additional origins (comma-separated).
CORS_EXTRA_ORIGINS=
CORS_ALLOWED_ORIGIN_REGEXES=
CSRF_EXTRA_ORIGINS=

# Azure OpenAI (required by openai_client.py – must use these names)
AZURE_ENDPOINT_URL="https://your-openai.cognitiveservices.azure.com/"
AZURE_DEPLOYMENT_NAME="gpt-5-mini"
AZURE_API_KEY="your-azure-openai-key"
AZURE_API_VERSION="2025-04-01-preview"

JIRA_EMAIL="your-jira-email"
JIRA_API_TOKEN="your-jira-api-token"
JIRA_DOMAIN="your-domain.atlassian.net"
JIRA_PROJECT_KEY="SCRUM"

AZURE_DEVOPS_ORGANIZATION="your-org"
AZURE_DEVOPS_PROJECT="your-project"
AZURE_DEVOPS_PAT="your-devops-pat"

# Neon PostgreSQL Database
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Local Redis (use when running Redis on Windows/WSL)
# REDIS_URL=redis://localhost:6379/0
# CELERY_BROKER_URL=redis://localhost:6379/1
# CELERY_RESULT_BACKEND=redis://localhost:6379/2

# Azure Redis Cache (SSL, port 6380) – cache, Celery broker, and result backend
REDIS_URL=rediss://:your-redis-key@your-redis-host.redis.cache.windows.net:6380/?ssl_cert_reqs=CERT_NONE
CELERY_BROKER_URL=rediss://:your-redis-key@your-redis-host.redis.cache.windows.net:6380/?ssl_cert_reqs=CERT_NONE
CELERY_RESULT_BACKEND=rediss://:your-redis-key@your-redis-host.redis.cache.windows.net:6380/?ssl_cert_reqs=CERT_NONE

# Redis SSL cert mode (Azure-managed Redis: optional/required/none)
REDIS_SSL_CERT_REQS=required

# In-memory cache fallback for tests / local dev
ALLOW_IN_MEMORY_CACHE=False

# Flower Configuration (optional)
FLOWER_BASIC_AUTH=admin:CHANGE_ME

# LLM concurrency: max concurrent API calls per analysis (default 8). Tune per Azure OpenAI TPM/RPM.
LLM_MAX_CONCURRENT_REQUESTS=8

# Local ML Pipeline Configuration
USE_LOCAL_PIPELINE=true

DEBUG=False
# Set true so GET /api/schema/ is registered (e.g. production API registry sync / CI). Swagger UI remains DEBUG-only.
ENABLE_OPENAPI_SCHEMA=false
# Canonical frontend URL — used to build links in digest emails, Slack install flow,
# auth redirects and billing portal return URLs. Must not have a trailing slash.
FRONTEND_BASE_URL=https://agent.saramsa.ai
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/your/webhook/url
TEST_USER_EMAIL=test.user@saramsa.local
TEST_USER_PASSWORD=your-test-password
LOGIN_EMAIL=test.user@saramsa.local
LOGIN_PASSWORD=your-test-password

# Linear API (Project Management)
LINEAR_API_KEY=your-linear-api-key

# Azure Communication Services Email
EMAIL_BACKEND=authentication.email_backend.AzureEmailBackend
AZURE_EMAIL_CONNECTION_STRING=endpoint=https://your-comm.communication.azure.com/;accesskey=your-key
DEFAULT_FROM_EMAIL=DoNotReply@your-domain.azurecomm.net

# Email / password-reset (SMTP fallback + reset email metadata)
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASSWORD=
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
PASSWORD_RESET_FROM_EMAIL=no-reply@saramsa.ai
PASSWORD_RESET_EMAIL_SUBJECT=Reset your Saramsa password

# Slack Source Connector (OAuth App)
BACKEND_BASE_URL=https://your-backend-url.azurewebsites.net
SLACK_CLIENT_ID=your-slack-client-id
SLACK_CLIENT_SECRET=your-slack-client-secret
SLACK_SIGNING_SECRET=your-slack-signing-secret
SLACK_REDIRECT_URI=https://your-redirect-url/api/integrations/slack/oauth/callback/

# Slack workspace pinning (set to enforce single-workspace integrations)
SLACK_TEAM_ID=

# Asana (PAT-based, customer-supplied) — only the webhook target URL is needed
# server-side. Customers mint a PAT in their own Asana account and paste it
# into the Saramsa UI; no Asana app registration on Saramsa's side.
ASANA_WEBHOOK_TARGET_URL=https://your-backend-url.azurewebsites.net/api/integrations/asana/webhook

# Stripe Billing (Subscriptions)
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_PUBLISHABLE_KEY=pk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
STRIPE_DEFAULT_PRICE_ID=price_xxx

# Per-month quotas (defaults if BillingProfile.metadata doesn't override)
QUOTA_ANALYSIS_PER_MONTH=50
QUOTA_WORK_ITEMS_PER_MONTH=100
QUOTA_LLM_TOKENS_PER_MONTH=500000

# Workspace invite TTL (days)
INVITE_TTL_DAYS=7

# Throttle rates (DRF rate-limit strings)
THROTTLE_RATE_ANON=
THROTTLE_RATE_USER=

# Slow-request thresholds (seconds)
SLOW_REQUEST_THRESHOLD=
VERY_SLOW_REQUEST_THRESHOLD=
